# 第一版核心功能进度

目标：让本地 GBA 游戏完成“导入 → 浏览 → 启动外部模拟器 → 返回原位置”的完整循环。

## 核心功能验收结论

第一版 GBA 核心循环已完成，当前构建复核结果如下。正式签名发布、自动化宏基准、在线刮削和更多平台仍是后续工作。

| 验收项 | 实现与验证依据 |
| --- | --- |
| 导入与增量扫描 | SAF 持久授权、Room 分批入库；真机 SD 卡拔出保留记录，插回后原授权直接恢复扫描 |
| 数据完整性 | 当前 11 项真机数据库测试通过，覆盖版本迁移、扫描失败与取消、去重、删除、收藏、元数据更新、历史结算及大库重复扫描 |
| 浏览与控制器操作 | 十字键短按、长按及松手停止；A/B/X/Y、肩键、搜索、排序和详情返回已有验收；最新导航来回与详情返回保持焦点 |
| 外部模拟器循环 | RetroArch G 显示游戏画面；用户确认实体按键退出后自动回到原游戏，设备记录确认模拟器进程退出 |
| 媒体与设置 | 本地封面、介绍、视频及播放偏好；锁屏前预暂停和亮屏恢复已通过真机检查，变暗时点的估算限制见下文 |
| 性能 | 应用 Baseline Profile 已打包，真机确认 speed-profile；同 APK 编译对照的启动与导航采样见下文，不作所有场景零掉帧保证 |
| 当前构建 | Debug、Release、测试 APK 和 lint 通过；68 项应用单元测试、4 项 Profile 转换测试通过 |

最后一轮数据库测试结束后，重新打开前端确认原有游戏库记录仍在。权限撤销测试需专用目录，本次未重复执行；既有权限失效验证与本轮实体插拔验收分别记录，不将跳过项计入通过数量。

## 已实现

- Android 系统桌面角色、Home 返回首页及单实例处理。
- SAF 多目录持久授权、递归扫描 `.gba` / `.zip`、分批入库、取消和增量删除检测。
- 成功扫描完成提示单次淡入：普通模式 160ms，减少动态模式 80ms；扫描计数、取消、失败和返回设置不重播，按钮与焦点不移动。
- 重叠目录按文档身份去重；成功扫描更新可用 URI。失败或中断的扫描不会清除旧目录数据。
- Room 保存游戏、收藏、最近游玩、启动次数、时长和待结算会话；版本升级保留数据。
- 首页、游戏库、详情、排序、收藏、最近游戏、名称与文件名搜索。
- 左栏顶部可在文字导航与窄图标栏之间切换；收起后仍可切换页面，收起状态只显示图标与选中边框，不弹出名称提示。DataStore 保存展开状态，外观设置同步。重复选择当前导航项保留页面、筛选与焦点。
- 搜索字符键盘支持方向键、A 输入、B 取消、Start 确认；触摸输入框可使用系统键盘。
- L1/R1 切换分类，L2/R2 快速翻页；首字母弹窗跳到当前排序的首个匹配游戏，# 包含数字及非拉丁文字。仅可见列表项创建焦点节点。
- 选择已安装模拟器，DataStore 保存平台对应配置；检查安装状态、ROM 可读性和 Intent 配置。
- ZIP 单 ROM 安全提取、体积和条目数限制、CRC 检查、失败或取消清理、FileProvider 限定分享。
- 启动与返回结算历史，保存页面、选中游戏和列表位置；新安装直接显示导入入口。

## 当前外观

- 固定使用暗色裂隙配色，移除亮色、跟随系统和主题配色切换。旧主题偏好不再生效。
- 外观设置仅保留减少动效和左侧导航展开开关。

## 历次验证记录

- Debug APK、单元测试、Android 测试 APK 和 Lint 构建通过。50 项单元测试通过；Lint 无错误，仍有依赖更新、资源及风格提示。
- 图标导航栏已验证连续折叠 10 次、当前页面重复确认、游戏库筛选保留、详情与设置焦点、外观开关同步和重启恢复；暗色及 4:3 小屏均已检查。
- 连续点击当前游戏库或设置导航保持画面与焦点不变，空库与重复确认首页也通过验证。
- 10 项 Room 设备测试通过：增量更新保留收藏与历史、删除检测、重叠目录去重与可用 URI 恢复、扫描失败、取消、5,000 条目重复扫描、会话只结算一次、版本 1 升级到版本 3、同名封面替换和移除。
- Android 35 测试环境：通过系统文件夹选择器导入 `.gba` 和 ZIP，实际扫描 5,002 条目。
- GBA.emu 1.5.85：原始 ROM 和 ZIP 提取文件都实际显示自制诊断程序的黄色与青色画面；最终封面版本再次启动成功。
- 诊断 ROM 无既有自动存档，GBA.emu 会短暂提示读取自动存档失败。模拟器的可写存档目录需单独配置；本轮未把存档写入或自动恢复列为已验证项。
- 运行模拟器期间终止前端后台进程，返回后恢复详情页、收藏、游戏选择和焦点，继续结算时长。

## 范围与待验收事项

- 重复检测覆盖同一文档的重叠目录授权；不同文档 ID 的相同内容副本仍保留为独立游戏。
- 扫描时按扩展名建立 ZIP 候选条目。通用模拟器启动路径在解压时检查条目、大小和 CRC；RetroArch 直接读取原始 ZIP，前端只检查文件可读性，压缩包内容由 RetroArch 处理。
- 时长是启动至返回的估算值，单次上限 24 小时；无法识别模拟器暂停或后台停留。Android 成功接收启动 Intent 不等于模拟器保证接受任意 ROM。
- 解压缓存保留 24 小时用于模拟器任务恢复，返回或再次准备时清理；系统也可能提前回收缓存。
- 本地同名 PNG、WebP、JPG、JPEG 封面已接入 Coil，按显示约束解码；实测替换同路径图片会更新，损坏图片回退占位图；Pegasus 本地元数据和视频预览已实现；在线元数据抓取、更多平台和完整 Android 应用抽屉不在当前核心循环内。
- 5,002 条目的真实 SAF 扫描、30 次连续快速翻页、手柄键盘搜索均已运行验证；未作帧率或输入延迟的量化保证。
- 临时移走授权目录时显示不可读提示并保留 5,002 条目；恢复目录并删除压力测试文件后，增量扫描正确回到两个诊断游戏。
- 搜索后焦点落到实际结果，B 取消保留原焦点，已在最终 APK 复验。
- 从详情进入设置移除目录后，返回详情显示游戏已移除提示；A 返回游戏库、B 返回上一页均已验证，取消移除保留记录。
- 空游戏库直接聚焦添加目录入口；上下键可往返筛选栏。系统选择器授权完成后，焦点落到新导入游戏，已通过运行断言。
- 首字母跳转在降序列表中准确定位并聚焦目标游戏；取消不会退出游戏库。
- 4:3、16:9、16:10 小屏画面均已检查布局与方向键焦点；排序按钮使用短标签，避免截掉排序方向。
- 禁用所选模拟器时显示重新选择入口，B 可返回；ROM 文件失效与模拟器配置错误分开提示。
- 真实 SAF 持久授权撤销测试通过：读取权限失效后保留记录与收藏，扫描报告不可读，ROM 读取被拒绝；已通过系统选择器重新授权。实体 SD 卡拔出与插回恢复已通过目标设备验收，见下文。
- 已验证空库导航、系统目录选择与取消、ROM 和同名封面导入、收藏重启持久化、详情及未配置模拟器提示；安装更新不改变默认桌面。
- KONKR Pocket Advance 已完成首轮实体方向键、长按连发、A/B/X/Y、Start、L1/R1 及 SD 卡目录扫描测试，模拟器内实体按键退出、自动返回及选择恢复也已验收；Release 配置的导航帧采样与编译状态对照见下文。

诊断 ROM 的源码和生成步骤见 [tools/test-fixtures](tools/test-fixtures/README.md)。不包含第三方游戏数据或 BIOS。

### RetroArch G 外部启动

- GBA 模拟器列表识别已安装的 `com.retroarch.aarch64`，选择后沿用现有 DataStore 模拟器映射持久化。
- 专用适配器使用 `RetroActivityFuture`，传递 `ROM`、mGBA 的 `LIBRETRO` 路径和现有 `retroarch.cfg`，与目标掌机 Pegasus G 的启动配置一致。
- Android 外部存储提供器的 SAF 文档转换为原始存储路径，启动前通过 SAF 检查可读性；不新增广泛存储权限。其他提供器暂不支持该适配器，仍可使用通用模拟器接口。
- RetroArch 直接读取原始 GBA / ZIP 文件，不使用 RiftDeck 的临时解压路径，保持其游戏和存档路径关联。核心、配置与存储读取权限由已有 RetroArch 安装提供；尚不自动安装或下载核心。
- 已在目标掌机验证：选择 RetroArch G · mGBA 后，从首页启动 SD 卡中的 ZIP 格式 ROM，成功显示游戏标题画面。构建、单元测试与 lint 通过。
- 切回 RiftDeck 后仍选中原游戏，最近游玩计数增加，重启安装后模拟器选择保留。补充真机验收：用户通过实体按键选择退出 RetroArch，前端自动恢复；设备记录确认 RetroArch 进程退出，用户确认仍选中刚启动的游戏，焦点恢复到开始游戏按钮。

### 本地 Pegasus 封面兼容

扫描器支持同目录同名图片、`media/游戏名/boxfront.*` 和 `mini_boxfront.*`，以及 `metadata.pegasus.txt` 中的 `file` / `files` 与 `assets.box_front` 映射。多版本 ROM 可以共用封面。只读取授权目录内的相对图片路径，不执行元数据中的命令，也不访问远程图片。元数据读取上限为 4 MiB；目录查询与图片信息匹配均在 IO 调度器执行，显示继续使用 Coil 的目标尺寸和版本缓存。

重新扫描验证：测试库中的游戏均匹配到本地封面；首页、游戏库、详情页显示正常，方向键切换、详情返回后焦点保持。兼容分卷子目录、多个版本共用图片、配置文件扩展名与实际图片不一致，以及失效图片路径按游戏名回退。构建、59 项单元测试与 lint 通过。

### 游戏介绍与视频预览

- 从 Pegasus 元数据导入 `description` 与 `assets.video`，兼容多版本、分卷目录和默认 `media/游戏名/video.mp4`。Room 升级至 v4，自动迁移保留已有游戏、收藏和游玩记录。
- 详情页新增「游戏介绍」入口，长文可触摸或用方向键滚动，B 返回并恢复按钮焦点。
- 设置 → 视频预览可切换封面图片与游戏视频，选择通过 DataStore 保存，默认图片。只替换当前大预览，列表缩略图继续显示封面。
- 使用 AndroidX Media3 ExoPlayer，默认静音循环，默认停留 650 ms 后启动。页面离开、后台、打开介绍和准备启动模拟器时释放播放器；播放失败或缺少视频时显示原封面。
- 目标掌机测试库中的游戏均导入介绍及视频路径。数据库 v3 → v4 更新成功，61 项单元测试通过；通过 Android instrumentation 验证迁移、扫描和元数据更新，报告 OK（12 tests）。

真机界面验收：图片/视频切换与重启持久化正常；长介绍方向键滚动正常，B 关闭后焦点回到「游戏介绍」；退到桌面时 Media3 日志确认播放器 Release，返回前端后按延迟重新启动。最终构建、单元测试及 lint 通过。


### 性能设置

性能页提供预览等待时间（0.65、1.2、2 秒）与视频循环开关，通过 DataStore 保存并作用于实际播放器。延长等待可避免快速浏览时启动解码；关闭循环后只播放一次，结束即释放播放器并恢复封面。设置不改变图片／视频显示选项，仅在启用视频预览时生效。

性能设置验收：方向键可往返两项控制，重启保留选择；循环开启时持续播放，关闭后完整播放一次并保持封面。构建、63 项单元测试和 lint 通过。


### 锁屏前暂停视频

根据系统锁屏超时与前端最近操作时间，按 Android 默认变暗窗口估算预暂停时点，并提前 250 ms 暂停视频；按键、触摸、手柄输入及亮屏解锁后恢复播放。预暂停保留播放器与当前位置，真正进入后台或锁屏时释放播放器，并在返回后恢复位置。单次预览已播放完成时不会因亮屏重新播放。

Android 没有公开且通用的锁屏前变暗事件，因此提前暂停是估算；厂商省电策略、系统管理策略和应用外操作可能改变实际变暗时间。熄屏、亮屏、解锁与窗口／生命周期事件用于补充控制，不修改亮度、锁屏超时或持有唤醒锁。

目标掌机验证：系统处于 DIM 状态时连续截图的视频帧保持不变，输入后恢复播放；真正熄屏并解锁后继续原片段。测试使用的临时锁屏超时已恢复。构建、66 项单元测试及 lint 通过。


### 模拟器配置恢复

DataStore 同时保存模拟器的 Intent action 和字符串 extras，重启后完整恢复启动参数；重新选择配置时替换旧参数。旧版本仅保存包名和 Activity 的记录继续使用默认 ACTION_VIEW 与空 extras。

### KPA 十字方向键复验

目标设备只有十字方向键，独立模拟摇杆验收不适用。实体输入记录确认连续五次向下、两次向上及约两秒长按；长按产生连续导航，释放后焦点保持稳定。方向键由设备通过 HAT 轴上报，沿用集中输入层处理。

### 实体 SD 卡插拔验收

正常退出外部模拟器并停止扫描、视频后，拔出目标掌机 SD 卡。系统确认存储离线；重新扫描显示目录不可读并保留全部已有记录，回到游戏库保持原选择位置。尝试启动离线 ROM 显示读取失败提示，可以关闭并继续导航。重新插卡后，原有持久授权可直接重新扫描，无需再次选择目录；扫描完成后条目数量不变、原游戏选择与列表位置保留，封面及视频预览恢复。随后设备进入 RetroArch G 并显示游戏启动画面。

### 当前构建复核

Release APK 构建通过，68 项单元测试全部通过，Debug lint 通过。当次 Release 产物未配置正式签名；后续使用测试证书签署并完成真机采样，结果见下文。构建成功本身不代表性能验收。

### Release 配置性能初测

使用测试证书签署的 Release 配置 APK，在目标掌机已有游戏库上采样。五次 `am start -S -W` 的 TotalTime 为 1015、916、898、909、915 ms，中位数 915 ms；这是进程重启后的 Activity 启动时间，不代表完全可操作时间或清空系统缓存后的启动时间。

连续注入三十次向下及三十次向上后，`dumpsys gfxinfo` 记录 360 帧，其中 90 帧超出截止时间（25%）；帧耗时 P50 为 12 ms、P90 为 38 ms、P95 为 46 ms，GPU P95 为 3 ms。结果提示需要进一步定位 UI 线程及帧调度开销，尚不能认定满足稳定 60 FPS；这组数据也不等同于实体按键输入延迟。仍需控制缓存与编译状态的重复基准、Baseline Profiles 和优化后的复测。

### 可见行焦点响应

可见且已创建焦点节点的游戏行现在直接请求焦点，不再额外等待一帧；屏幕外的行仍等待滚动和布局完成。Release 构建、单元测试及 lint 通过。目标掌机连续三十次向下、三十次向上后恢复原游戏，详情返回也保持原焦点。单轮复测记录 358 帧、77 帧超时（21.51%），P95 仍为 46 ms；该结果不能证明整体性能提升，剩余掉帧仍需定位。

### 导航系统跟踪

对相同方向键导航流程采集系统跟踪：主线程焦点请求最大约 1.3 ms，较长切片集中在 Compose 布局与重组，单个 Choreographer 帧最大约 37.8 ms。跟踪中还包含无障碍语义树更新；检查确认设备启用了厂商无障碍服务，并非仍有 UIAutomator 连接。测试保留该服务，未为改善数字而关闭设备功能。切片时间包含线程被调度出去的时间且有嵌套，不能直接相加作为 CPU 用时。后续需区分实际执行和调度等待，并采用更低干扰的输入采样。

### 单进程输入基线

新增 [导航帧采样工具](tools/performance/README.md)，在单个 shell 进程中注入三十次向下、三十次向上，释放间隔 145 ms，不打包进应用。目标掌机三轮均回到原游戏；超时帧分别为 45/418（10.77%）、42/410（10.24%）、34/416（8.17%），P95 分别为 29、30、23 ms。结果仍不满足稳定帧率要求。由于改变了输入工具和时序，不能把与之前逐键命令的差异认定为应用优化收益；后续优化使用此流程作同条件对照。

### 编译与设备服务状态对照

调用按运行 Profile 编译的系统命令返回成功，但后续 ART 查询仍显示 `run-from-apk`，因此不能认定预编译已生效。随后三轮同工具导航记录超时帧 5/440（1.14%）、2/418（0.48%）、3/417（0.72%），P95 均为 12 ms，焦点仍回到原游戏。与此同时，厂商无障碍服务从已绑定变为未绑定（启用配置仍在；测试未修改服务设置）。这属于环境混杂因素，不能将结果归因于编译或代码优化；仍需服务正常绑定条件下的对照。当时设备拒绝导出运行 Profile 二进制；随后通过可读运行记录生成了应用规则，见下一节。

### 应用 Baseline Profile 初版

从 Android 12 可读运行记录转换得到 1,582 条精确应用类与方法规则，加入 `app/src/main/baseline-prof.txt`；无包级通配符，保留 H/S/P 标记，依赖库规则由构建合并。转换器四项测试通过，Release 构建成功，APK 包含 10,831 字节的 baseline.prof 与 702 字节的 baseline.profm。真机覆盖安装成功；显式安装广播未返回预期成功码，但启动应用后执行按 Profile 编译，ART 状态已由 verify 变为 speed-profile（cmdline）。这证明设备具备按 Profile 编译的状态，尚未隔离本次应用规则与依赖规则的性能收益。

### 同 APK 编译状态对照

使用同一 Release APK、同一库位置、同一单进程输入工具，分别强制 verify 和 speed-profile 后采样；两组前后厂商无障碍服务均保持绑定，ART 查询确认实际编译状态。verify 三次进程重启启动为 292、295、295 ms，导航三轮超时帧为 19.24%、12.50%、10.50%，P95 为 48、34、28 ms。speed-profile 启动为 196、202、193 ms，导航超时帧为 1.36%、0.72%、1.41%，P95 为 12、12、17 ms；两组导航后均恢复原游戏。

这组结果支持按 Profile 编译可改善当前构建的启动和导航表现，但包含应用与依赖规则、运行期 Profile 的共同作用，不单独证明新增规则的收益。采样顺序固定，未清空文件缓存；仍不构成所有场景稳定 60 FPS 的保证。
